# 仿真金融市场沙盒：项目介绍

仿真金融市场沙盒是一个可审计的金融仿真实验工作台，用于构造边界明确、信息不完全、参与者异质的本地现货市场，让研究者从同一个初始状态出发，观察不同外生事件、信息传播路径和 Agent 决策如何共同形成不同的市场轨迹。一次实验可以被保存、精确回看、从检查点分叉，并与另一条分支对齐比较。

## 项目要解决的问题

传统市场仿真常在两个方向上失真：一种是无法正确处理同质规则简化与自主权交给AI Agent之间的边界，另一种是AI做行为决策需要等待时间，无法做到实时监督市场并生成策略。

本项目将Agent解耦为AI策略规划器与动作选择器，AI策略规划起到现实中人类前额叶的作用，完成对未来的规划，而动作选择器则根据对当前市场的状态观察与规划的策略实时选择动作执行。这符合真实市场中人类的行为，人们通常有个基础规划，之后根据市场价格走势做出不同选择。

金融沙盒是完整的基础设施，具有面向Agent的接口，动作选择器通过标准接口与沙盒交互，之后沙盒根据不同Agent的不同行为进行演变。

## 一次实验如何展开

用户先选择场景的链与目标资产、Agent数量、模型 Provider、市场参数和随机种子。外部链数据只用于冻结初始化输入；目标 Token、合成报价资产 USDx、显式 Agent 账户和背景市场库存经过逐资产守恒校验后，形成可确认的 Resolved Initial State。

场景确认后，根分支沿虚拟时间自主推进。一个完整循环遵循固定顺序：

```text
权威世界事件
  -> 权限、送达、延迟与注意力筛选
  -> 保存某个世界版本下的 ObservationPacket
  -> 形成去重的 Agent 决策机会
  -> 更新记忆与信念、评估计划、提出动作
  -> 校验权限和资源，建立 Reservation 与 Pending Action
  -> 通过 Ledger、CLOB、Information 或 Social 模块执行
  -> 生成回执和新事件
  -> 进入下一次观察屏障
```

运行中可以请求 Pause、Save 或 Stop；系统在完整事件与观察屏障结束后进入确定边界。暂停时，用户可以让 Scenario Director 把自然语言意图解释成类型化干预草案，检查必要的因果前提、信息披露范围和预期影响，再决定确认或拒绝。确认后的当前阶段原子提交，未来阶段进入调度队列；恢复运行后，Agent 才会从各自可见的观察出发作出响应。

保存点可以产生隔离的子分支。两个分支继承分叉前的历史与状态，分叉后的账户、Agent 认知、事件队列、随机流和 Provider 结果相互隔离。用户可以向两个分支加入不同事件，在相同虚拟时间或事件游标上比较价格、流动性、信息扩散和参与者行为的差异。

## 金融世界

产品聚焦一个目标 Token、一个合成 USDx 和一个本地现货中央限价订单簿。Ledger 拥有账户、余额、锁定和记账分录；CLOB 拥有订单、队列和成交。任何交易都必须先有真实余额，经过资源预留，再按价格时间优先和滑点保护规则撮合、结算。

背景市场部门代表未被显式建模的市场活动，但它不是 Agent，也没有 Persona、记忆。它使用显式 Agent 分配后剩余的、已记账的 Token 与 USDx，通过对用户输入事件的影响进行评估，而进行相应价格走势。

## Agent 与局部认知

每个显式 Agent 都是独立的认知与决策主体。它拥有隔离的 Persona、Observation、Memory、Belief、注意力与认知预算、策略状态、账户投影和命名随机流。用户看到的“普通参与者”“资本型持有者”“流动性提供者”“资产发行方”或“信息参与者”只是可组合的初始化预设与分析标签，不是运行时子类，也不会自动决定财富、权限或行为。

所有 Agent 共用同一条固定决策流水线，但可以在风险容忍度、时间范围、怀疑程度、沟通倾向、初始私有知识和规划方式上不同。Role Tag 用于描述和分析，Capability 才是硬权限，Portfolio 才表达资产。LLM 只在受限边界内生成结构化候选计划；它不能直接写账本、修改世界、执行代码、读取其他 Agent 的私有状态，或绕过确定性的 Reactive Controller 生成权威动作。

Agent 只根据已经送达、被注意并保存在 ObservationPacket 中的内容决策。系统明确区分信息的创作或发布、送达、查看、记住、相信和据此行动。公开发布不等于所有 Agent 立即知情，私聊也不等于即时送达或阅读。Agent 可以公开表达、选择性披露、保持沉默，甚至发布与自身私有判断相反的策略性主张；接收者只能看到被披露的内容，并依据自己的怀疑程度和证据形成主观信念。系统保存信息派生关系，但不设一个全局权威的“信任分数”。


## 产品形态

沙盒面向情景设计者、市场研究与分析人员、Agent 设计者以及负责验证运行正确性的工程人员。产品界面围绕“准备、运行、分析、保存与分叉”组织：

- 准备阶段回答“这个实验实际会创建什么”，展示来源、Agent 分布、资产守恒、初始盘口和容量风险。
- 市场与事件视图回答“世界正在发生什么”，展示虚拟时间、盘口、成交、事件和运行边界。
- Agent 视图回答“它当时看见、相信、计划并执行了什么”，串联观察、记忆、信念、计划、决策、Reservation、Pending Action 与回执。
- 信息视图回答“内容如何创作、送达、查看和派生”，避免把发布误解为全员知情。
- 干预与分支视图回答“外部事件改变了什么、何时生效，以及两个世界从哪里开始不同”。


## 边界

当前范围有意保持克制：单链初始化、单目标资产、单报价资产、单现货 CLOB、本地单所有者和同运行时版本的存档恢复。完整可执行链 fork、智能合约执行、AMM、永续与期权、多链多资产、多市场、真实身份数字孪生、强化学习或自由插件 Agent、生产级多租户云部署以及跨版本自动迁移均不属于当前承诺。


